import { OdeChapterPage, odeChapterMetadata } from "../OdeChapterPage";

export const metadata = odeChapterMetadata(5);

export default function OdeChapter5Page() {
  return <OdeChapterPage chapter={5} />;
}
