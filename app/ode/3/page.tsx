import { OdeChapterPage, odeChapterMetadata } from "../OdeChapterPage";

export const metadata = odeChapterMetadata(3);

export default function OdeChapter3Page() {
  return <OdeChapterPage chapter={3} />;
}
