import { OdeChapterPage, odeChapterMetadata } from "../OdeChapterPage";

export const metadata = odeChapterMetadata(4);

export default function OdeChapter4Page() {
  return <OdeChapterPage chapter={4} />;
}
