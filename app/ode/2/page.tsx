import { OdeChapterPage, odeChapterMetadata } from "../OdeChapterPage";

export const metadata = odeChapterMetadata(2);

export default function OdeChapter2Page() {
  return <OdeChapterPage chapter={2} />;
}
