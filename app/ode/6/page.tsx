import { OdeChapterPage, odeChapterMetadata } from "../OdeChapterPage";

export const metadata = odeChapterMetadata(6);

export default function OdeChapter6Page() {
  return <OdeChapterPage chapter={6} />;
}
